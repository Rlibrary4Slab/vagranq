
<%#= image_tag @user.gravatar_url(length: 100), class: 'img-responsive img-rounded'%>
 
<%#= "#{@user.user_name}@#{@user.screen_name}"%>
<%= @user.user_id}%>@<%= @user.user_name%>
<%= @user.user_description%>
 記事一覧
<% @user.articles.published.each do |article| %>
  <%= u_article_path(@user, article)%>
    <%= article.title%>
      
        <%= truncate(article.description) %>
        <%= safe_date_localize(article.published_at)%>
<%end%>
