<%= form_for(@article) do |f| %>
  <% if @article.errors.any? %>
    <div id="error_explanation">
      <!--<h2><%= pluralize(@article.errors.count, "error") %> 個項目がありません</h2>-->

      <ul>
      <% @article.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div id="posting_buttons">
  <input type="button" class="tdsubmit btn btn-lg btn-default" value="↑↓">
  <%#= f.button '下書き保存', name: 'ope[cmd]', value: 'save', class: 'btn btn-lg btn-default'%>
  <%= f.button @article.new_record? ? '下書き保存' : '変更する', name: 'ope[cmd]
', value: 'save', class: 'btn btn-lg btn-default' %>

  <%if @article.published? %>
    <%= f.button '下書き保存！', name: 'ope[cmd]', value: 'draft', class: 'btn b
tn-lg btn-default'%>

  <%end%>
  <%if @article.draft?%>
    <%= f.button '公開', name: 'ope[cmd]', value: 'publish', class: 'btn btn-lg btn-default'%>
  <% end %>
  </div> 
   <%#= f.check_box :checkagree ,:id =>"checkAgree"%> 
   <%= f.hidden_field :user_id, value: current_user.id %>
    <%= f.text_field :eyecatch_img, :style =>"display:none;" ,:id => "eyecatch_img"%>
     <div style="display:none;">
       <input type="button" id="tsubmit" value="交換">
       <input type="button" id="dsubmit" value="交換">
       <input type="button" id="csubmit" value="checked">
       <input type="button" id="tansubmit" value="ansubmit"> 
       <input type="button" id="dansubmit" value="ansubmit"> 
       <!--<input type="checkbox" id="checkAgree">-->
       <%= f.check_box :checkagree ,:id =>"checkAgree"%> 
        
     </div>
    <div class="field">
      <%= f.select :category ,Article.categories.keys.to_a%></div>
    <div class="field">
      <%= f.label :title %><!--<input type="button" class="tdsubmit btn btn-default" value="交換">--><br>
      <%= f.text_field :title %>
    </div>
    <div class="field" style="display:none;">
      <%= f.label :description %><br>
      <%= f.text_field :description %>
    </div>
  <input type="text" style="display:none;" id="iurtmp" />
   
   <div class="field sortable">
     <ul class="ranking-content">

        <%= f.fields_for :contents do |builder| %>
             <%= render "content_fields", f: builder  %>
        <% end %>
     
   
    <%= link_to_add_fields "+", f, :contents %>
    </ul>
   </div>
  <input type="button" class="tdsubmit btn btn-lg btn-default" value="↑↓">
  <%#= f.button '下書き保存', name: 'ope[cmd]', value: 'save', class: 'btn btn-lg btn-default'%>
  <%= f.button @article.new_record? ? '下書き保存' : '変更する', name: 'ope[cmd]', value: 'save', class: 'btn btn-lg btn-default' %>

  <%if @article.published? %>
    <%= f.button '下書き保存！', name: 'ope[cmd]', value: 'draft', class: 'btn btn-lg btn-default'%>
    
  <%end%>    
  <%if @article.draft?%>
    <%= f.button '公開', name: 'ope[cmd]', value: 'publish', class: 'btn btn-lg btn-default'%>
    
  <%end%>
  <%#= f.submit 'このまま保存する', :id => "fullput",:style =>"display",:class => 'btn btn-primary' %>
  <%#= link_to t('.cancel', :default => t("helpers.links.cancel")),
            articles_path, :class => 'btn btn-lg btn-default' %>
  
  
<% end %>
