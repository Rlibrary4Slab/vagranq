   
    <% if current_user.likes.find_by(article_id: article.id) %>
      <%= link_to image_tag('heart_1.png', { :size => "30", :border => '0', :alt => ''}), unlike_path(article.id), method: :delete, id: "like-link",remote: true %>
    <% else %>
      <%= link_to image_tag('heart_0.png', { :size => "30",:border => '0', :alt => ''}), like_path(article.id), method: :post, id: "like-link",remote: true %>
    <% end %>
  
