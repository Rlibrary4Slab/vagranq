   
    <% if current_user.likes.find_by(article_id: article.id) %>
      <%= link_to 'いいね！を取り消す', unlike_path(article.id), method: :delete, id: "like-link",remote: true %>
    <% else %>
      <%= link_to 'いいね！', like_path(article.id), method: :post, id: "like-link",remote: true %>
    <% end %>
  
