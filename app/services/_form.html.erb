<%= form_for(@article) do |f| %>
  <% if @article.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@article.errors.count, "error") %> prohibited this article from being saved:</h2>

      <ul>
      <% @article.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
    <div class="field">
      <%= f.select :category ,Article.categories.keys.to_a%></div>
    <div class="field">
      <%= f.label :title %><br>
      <%= f.text_field :title %>
    </div>
    <div class="field">
      <%= f.label :description %><br>
      <%= f.text_area :description %>
    </div>
    
    
    <div class="form-modal" >
      <div class="modal">
        <div id ="article-form">
          
          <div class="field">
            <%= f.label :content %><br>
            <%= f.text_area :content%>
          </div>
          <div id="submit-btn">追加</div>
          <div id="cancel-btn" style="cursor:pointer;">キャンセル</div>
        </div>
      </div>  
    </div>
  <div class="actions">
    <%= f.submit %>
  </div>
  
<% end %>
